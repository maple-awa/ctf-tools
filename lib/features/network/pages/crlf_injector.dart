import 'package:flutter/material.dart';

class CRLFInjectorScreen extends StatelessWidget {
  const CRLFInjectorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('CRLF 注入检测 - CRLF Payload 生成与检测'),
    );
  }
}
