import 'package:flutter/material.dart';

class XXEGeneratorScreen extends StatelessWidget {
  const XXEGeneratorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('XXE 漏洞利用 - XXE Payload 生成器'),
    );
  }
}
