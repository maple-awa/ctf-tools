import 'package:flutter/material.dart';

class ShellcodeAnalyzerScreen extends StatelessWidget {
  const ShellcodeAnalyzerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Shellcode 分析 - Shellcode 反汇编与执行'),
    );
  }
}
