import 'package:flutter/material.dart';

class PEParserScreen extends StatelessWidget {
  const PEParserScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PE 文件解析 - Windows PE 文件分析'),
    );
  }
}
