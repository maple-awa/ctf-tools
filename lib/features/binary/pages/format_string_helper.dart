import 'package:flutter/material.dart';

class FormatStringHelperScreen extends StatelessWidget {
  const FormatStringHelperScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('格式字符串漏洞辅助 - Format String Payload 生成'),
    );
  }
}
