import 'package:flutter/material.dart';

class ELFParserScreen extends StatelessWidget {
  const ELFParserScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ELF 文件解析 - ELF 文件头、段、符号表解析'),
    );
  }
}
