import 'package:flutter/material.dart';

class RSAToolkitScreen extends StatelessWidget {
  const RSAToolkitScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('RSA 工具 - 密钥解析、加解密、签名验证'),
    );
  }
}
