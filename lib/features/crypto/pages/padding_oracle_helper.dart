import 'package:flutter/material.dart';

class PaddingOracleHelperScreen extends StatelessWidget {
  const PaddingOracleHelperScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Padding Oracle 攻击 - 填充预言攻击辅助工具'),
    );
  }
}
