import 'package:flutter/material.dart';

class HashLengthExtensionScreen extends StatelessWidget {
  const HashLengthExtensionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('哈希长度扩展攻击 - Hash Length Extension 攻击辅助'),
    );
  }
}
