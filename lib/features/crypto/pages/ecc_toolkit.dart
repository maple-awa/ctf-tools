import 'package:flutter/material.dart';

class ECCToolkitScreen extends StatelessWidget {
  const ECCToolkitScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ECC 椭圆曲线工具 - 曲线参数解析、加解密'),
    );
  }
}
