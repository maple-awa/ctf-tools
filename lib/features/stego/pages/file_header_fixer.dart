import 'package:flutter/material.dart';

class FileHeaderFixerScreen extends StatelessWidget {
  const FileHeaderFixerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('文件头尾修复工具 - 损坏文件修复'),
    );
  }
}
