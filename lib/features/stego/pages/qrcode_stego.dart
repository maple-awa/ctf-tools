import 'package:flutter/material.dart';

class QRCodeStegoScreen extends StatelessWidget {
  const QRCodeStegoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('二维码隐写工具 - QR Code 数据提取与隐藏'),
    );
  }
}
