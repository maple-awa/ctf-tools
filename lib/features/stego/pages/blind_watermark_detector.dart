import 'package:flutter/material.dart';

class BlindWatermarkDetectorScreen extends StatelessWidget {
  const BlindWatermarkDetectorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('盲水印检测工具 - 频域盲水印提取'),
    );
  }
}
